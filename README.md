**This is car project**
*to start do folowing comands*
```
python -m venv venv 
venv/Scripts/Activate.ps1
pip install -r requirements.txt
python manage.py migrate 
python manage.py makemigrations 
python manage.py runserver 
```

**Good Luck**
